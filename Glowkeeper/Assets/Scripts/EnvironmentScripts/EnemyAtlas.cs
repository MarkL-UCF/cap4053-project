using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyAtlas : MonoBehaviour
{
    //This script is meant to do nothing on its own. It is simply a way to store references to enemy prefabs via IDs
    //No duplicate entries please

    public GameObject[] eAtlas; //stores the enemies

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
